# function คือ การทำงานหนึ่ง จะทำงานเมื่อถูกเรียกใช้ (call function)
# ที่มาของ function 
# 1. built-in function / standard เป็รฟังก์ชันที่มีมาให้แล้ว ไม่ต้องสร้างเอง สนใจแค่ใช้งาน
# 2. user-defined function คือ ฟังก์ชันที่เราสร้างขึ้นมาเอง
# 3. Third party function คือ ฟังก์ชันที่มีคนอื่นสร้างไว้แล้ว เราแค่เรียกใช้งาน

# ประเภทของ function
# no parameter no return
# have parameter no return
# no parameter have return
# have parameter have return